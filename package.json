{
  "name": "dbom-core",
  "private": true,
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1"
  },
  "scripts": {
    "validate": "ajv validate -s schema/dbom-v0.schema.json -d 'examples/*.json' --spec=draft2020 -c ajv-formats",
    "dbom:validate": "node scripts/dbom.mjs validate",
    "dbom:query": "node scripts/dbom.mjs query",
    "spdx2dbom": "node scripts/interop-spdx.mjs"
  },
  "type": "module",
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "minimist": "^1.2.8"
  }
}